# Deep Learning Lab - Requirements
# Python 3.8+ required

# Dataset Management
kaggle>=1.5.16

# Core Deep Learning Frameworks
torch>=2.0.0
torchvision>=0.15.0

# TensorFlow (Optional - only needed for specific labs)
# Note: TensorFlow may not be available for all platforms (e.g., Apple Silicon)
# The main labs (1-3) use PyTorch only
# tensorflow>=2.12.0; platform_machine != "arm64"
# keras>=2.12.0; platform_machine != "arm64"

# Computer Vision
opencv-python>=4.8.0
Pillow>=10.0.0

# Scientific Computing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Machine Learning
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Image Segmentation & Object Detection
# Note: Labs 5-6 use custom implementations, no external libraries needed
# Optional advanced libraries (not required):
# segmentation-models-pytorch>=0.3.0
# ultralytics>=8.0.0  # For production YOLO
# detectron2  # For Mask R-CNN (requires separate installation)

# Natural Language Processing (Labs 7-8, 10)
# Note: Labs use custom implementations, these are optional for advanced features
# nltk>=3.8.0
# transformers>=4.30.0
# tokenizers>=0.13.0

# Time Series (Lab 9)
# Note: Lab uses custom LSTM/GRU, statsmodels is optional
# statsmodels>=0.14.0

# Utilities
tqdm>=4.65.0
jupyter>=1.0.0
ipython>=8.12.0

# Data Handling
h5py>=3.9.0
pycocotools>=2.0.6  # For COCO format

# Optional: For faster training
# tensorboard>=2.13.0
# wandb>=0.15.0

# Development Tools
pytest>=7.4.0
black>=23.7.0
flake8>=6.1.0

# Note: Some packages may require additional system dependencies
# For GPU support, install CUDA-compatible versions of PyTorch
# Visit: https://pytorch.org/get-started/locally/

# Installation:
# pip install -r requirements.txt

# All Labs 1-10 work with the core dependencies above!
# Labs 4-10 use custom implementations and synthetic data.
# No external datasets or additional packages required.

# Optional packages for advanced features (not needed for labs):
# Lab 4: pip install labelImg labelme  # For real annotation tools
# Lab 5: pip install segmentation-models-pytorch  # For pre-trained models
# Lab 6: pip install ultralytics  # For production YOLO
# Lab 7-8: pip install transformers nltk  # For pre-trained NLP models
# Lab 9: pip install statsmodels  # For statistical forecasting methods

# Recommended: Use requirements-minimal.txt for faster installation
# pip install -r requirements-minimal.txt